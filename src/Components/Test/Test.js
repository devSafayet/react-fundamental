/* import React, { useState } from 'react';

const Test = ({ Count }) => {
    const [count, setCount] = useState(0);
    const Pluse = () => {
        setCount(count + 1)
    }
    const Minus = () => {
        setCount(count - 1)
    }
    return (
        <div>
            <div className="count mt-5">
                <button onClick={Pluse} className="ms-3 p-3">+</button>
                <button onClick={Minus} className="ms-3 p-3">-</button>
            </div>
        </div>
    );
};

export default Test; */